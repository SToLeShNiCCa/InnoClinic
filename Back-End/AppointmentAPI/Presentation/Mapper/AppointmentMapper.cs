using AutoMapper;

namespace Presentation.Mapper
{
    public class AppointmentMapper : Profile
    {
        public AppointmentMapper()
        {
            CreateMap<>();
            CreateMap<>();

            CreateMap<>();
            CreateMap<>();

            CreateMap<>();
            CreateMap<>();
        }
    }
}
